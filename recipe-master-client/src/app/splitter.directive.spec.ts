import { SplitterDirective } from './splitter.directive';

describe('SplitterDirective', () => {
  it('should create an instance', () => {
    const directive = new SplitterDirective();
    expect(directive).toBeTruthy();
  });
});
